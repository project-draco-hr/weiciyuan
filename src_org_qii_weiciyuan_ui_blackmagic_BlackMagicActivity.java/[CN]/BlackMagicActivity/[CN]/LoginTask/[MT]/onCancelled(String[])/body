{
  super.onCancelled(s);
  if (progressFragment != null) {
    progressFragment.dismissAllowingStateLoss();
  }
  if (e != null)   Toast.makeText(BlackMagicActivity.this,e.getError(),Toast.LENGTH_SHORT).show();
}
