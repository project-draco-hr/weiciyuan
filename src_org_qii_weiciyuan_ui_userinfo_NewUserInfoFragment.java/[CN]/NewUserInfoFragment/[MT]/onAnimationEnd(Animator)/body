{
  if (getActivity() == null) {
    return;
  }
  displayCoverPicture();
  if (isMyself() && isOpenedFromMainPage()) {
    readDBCache();
  }
 else {
    fetchLastestUserInfoFromServer();
    loadNewMsg();
    fetchTopicInfoFromServer();
  }
}
