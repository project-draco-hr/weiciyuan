{
  WeiboMsg msg=homeList.getStatuses().get(0);
  String since_id="";
  if (msg != null) {
    since_id=msg.getId();
  }
  return new FriendsTimeLineMsgDao(token).setSince_id(since_id).getGSONMsgList();
}
