{
synchronized (TaskCache.backgroundWifiDownloadPicturesWorkLock) {
    downloadTasks.remove(url,downloadWorker);
    if (TaskCache.isDownloadTaskFinish())     TaskCache.backgroundWifiDownloadPicturesWorkLock.notifyAll();
  }
}
