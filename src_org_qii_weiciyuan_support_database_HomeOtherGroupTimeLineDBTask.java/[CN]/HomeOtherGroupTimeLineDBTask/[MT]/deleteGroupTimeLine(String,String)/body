{
  String sql="delete from " + HomeOtherGroupTable.TABLE_NAME + " where "+ HomeOtherGroupTable.ACCOUNTID+ " in "+ "("+ accountId+ ")"+ " and "+ HomeOtherGroupTable.GROUPID+ " = "+ groupId;
  getWsd().execSQL(sql);
}
