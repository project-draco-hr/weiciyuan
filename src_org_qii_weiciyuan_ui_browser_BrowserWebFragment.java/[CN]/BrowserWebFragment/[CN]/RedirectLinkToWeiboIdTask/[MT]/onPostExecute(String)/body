{
  super.onPostExecute(id);
  if (getActivity() == null) {
    return;
  }
  if (Long.valueOf(id) > 0L) {
    startActivity(BrowserWeiboMsgActivity.newIntent(id,GlobalContext.getInstance().getSpecialToken()));
    getActivity().finish();
  }
 else {
    Toast.makeText(GlobalContext.getInstance(),R.string.cant_not_convert_to_weibo_id,Toast.LENGTH_SHORT).show();
    mWebView.loadUrl(oriUrl);
  }
}
