{
  super.onCancelled(commentListBean);
  cleanWork();
  ((TextView)footerView.findViewById(R.id.listview_footer)).setText(getString(R.string.more));
  if (this.e != null)   Toast.makeText(getActivity(),this.e.getError(),Toast.LENGTH_SHORT).show();
}
