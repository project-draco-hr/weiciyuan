{
  if (mActionMode != null) {
    mActionMode.finish();
    mActionMode=null;
  }
  if (getListView().getCheckedItemCount() > 0) {
    getListView().clearChoices();
    getAdapter().notifyDataSetChanged();
  }
}
