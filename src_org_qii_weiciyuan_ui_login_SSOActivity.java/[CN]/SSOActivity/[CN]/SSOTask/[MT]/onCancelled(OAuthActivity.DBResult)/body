{
  super.onCancelled(dbResult);
  if (progressFragment != null) {
    progressFragment.dismissAllowingStateLoss();
  }
  if (e != null)   Toast.makeText(SSOActivity.this,e.getError(),Toast.LENGTH_SHORT).show();
}
