{
  super.onCancelled(commentBean);
  if (this.e != null) {
    Toast.makeText(AbstractNewActivity.this,e.getError(),Toast.LENGTH_SHORT).show();
  }
  if (progressFragment != null)   progressFragment.dismissAllowingStateLoss();
}
