{
  ContentValues cv=new ContentValues();
  cv.put(AccountTable.ID,account.getUid());
  cv.put(AccountTable.OAUTH_TOKEN,account.getAccess_token());
  long result=wsd.insert(AccountTable.TABLE_NAME,AccountTable.ID,cv);
  return result;
}
