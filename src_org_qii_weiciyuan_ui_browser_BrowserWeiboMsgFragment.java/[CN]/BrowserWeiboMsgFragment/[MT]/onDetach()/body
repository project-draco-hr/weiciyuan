{
  super.onDetach();
  if (task != null) {
    task.cancel(true);
  }
  if (geoTask != null) {
    geoTask.cancel(true);
  }
  avatar.setImageDrawable(null);
  content_pic.setImageDrawable(null);
  repost_pic.setImageDrawable(null);
}
