{
  if (!TextUtils.isEmpty(listViewSpannableString)) {
    return listViewSpannableString;
  }
 else {
    ListViewTool.addJustHighLightLinks(this);
    if (reply_comment != null)     reply_comment.getListViewSpannableString();
    return listViewSpannableString;
  }
}
