{
switch (item.getItemId()) {
case R.id.menu_refresh:
    if (task == null || task.getStatus() == AsyncTask.Status.FINISHED) {
      task=new SimpleTask();
      task.execute();
    }
  break;
}
return true;
}
