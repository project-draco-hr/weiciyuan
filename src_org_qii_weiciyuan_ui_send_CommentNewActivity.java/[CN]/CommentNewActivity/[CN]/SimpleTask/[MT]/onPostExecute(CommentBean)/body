{
  progressFragment.dismissAllowingStateLoss();
  if (s != null) {
    finish();
    Toast.makeText(CommentNewActivity.this,"????????????",Toast.LENGTH_SHORT).show();
  }
 else {
    Toast.makeText(CommentNewActivity.this,"failed",Toast.LENGTH_SHORT).show();
  }
  super.onPostExecute(s);
}
