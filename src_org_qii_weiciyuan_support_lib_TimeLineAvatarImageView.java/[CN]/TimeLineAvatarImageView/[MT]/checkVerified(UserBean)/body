{
  if (user != null && user.isVerified() && !TextUtils.isEmpty(user.getVerified_reason())) {
    if (user.getVerified_type() == 0) {
      verifiedPersonal();
    }
 else {
      verifiedEnterprise();
    }
  }
 else {
    reset();
  }
}
