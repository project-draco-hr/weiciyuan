{
  super.onCancelled(newValue);
  if (this.e != null) {
    Toast.makeText(getActivity(),e.getError(),Toast.LENGTH_SHORT).show();
    showErrorFooterView();
  }
 else {
    dismissFooterView();
  }
}
