{
  DMUserListFragment fragment=((DMUserListFragment)getSupportFragmentManager().findFragmentByTag(DMUserListFragment.class.getName()));
  if (fragment == null) {
    fragment=new DMUserListFragment();
    fragment.setArguments(new Bundle());
  }
  return fragment;
}
