{
  CommentsTimeLine fragment=((CommentsTimeLine)getSupportFragmentManager().findFragmentByTag(CommentsTimeLine.class.getName()));
  if (fragment == null) {
    fragment=new CommentsTimeLine();
    fragment.setArguments(new Bundle());
  }
  return fragment;
}
