{
  progressFragment.dismissAllowingStateLoss();
  if (s != null) {
    finish();
    Toast.makeText(RepostNewActivity.this,"????????????",Toast.LENGTH_SHORT).show();
  }
 else {
    Toast.makeText(RepostNewActivity.this,"failed",Toast.LENGTH_SHORT).show();
  }
  super.onPostExecute(s);
}
